[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


[functions.sendNotUpdatedSheets]
  schedule = "0 10 * * 2,5"  # at 18:00 on Tuesday (2) and Friday (5)